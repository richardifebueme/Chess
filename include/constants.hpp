#pragma once

constexpr int WINDOW_WIDTH = 1000;
constexpr int WINDOW_HEIGHT = 1000;
constexpr int BOARD_WIDTH = 800;
constexpr int BOARD_HEIGHT = 800;
constexpr int BOARD_X = (WINDOW_WIDTH / 2) - (BOARD_WIDTH / 2);
constexpr int BOARD_Y = (WINDOW_HEIGHT / 2) - (BOARD_HEIGHT / 2);
constexpr int ROW_SIZE = 8;
constexpr int CELL_WIDTH = BOARD_WIDTH / ROW_SIZE;
constexpr int CELL_HEIGHT = BOARD_HEIGHT / ROW_SIZE;
